#!/usr/bin/env zsh
# vim: ft=zsh

unsetopt BEEP

# Source modular config files
for conf in "${HOME}/.config/zsh/"*.zsh(N) "${HOME}/.config/zed/zsh/"*.zsh(N); do
    source "$conf"
done

bindkey -e

# Initialize starship prompt
eval "$(starship init zsh)"
