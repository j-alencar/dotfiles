set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

bind -n S-Left previous-window
bind -n S-Right next-window

bind -n M-H previous-window
bind -n M-L next-window

set -g status-style "bg=#282828,fg=#ebdbb2"
set -g status-left "#[bg=#cc241d,fg=#ebdbb2,bold] #S #[bg=#282828] "
set -g status-right "#[bg=#928374,fg=#ebdbb2] %Y-%m-%d #[bg=#b8bb26,fg=#282828,bold] %H:%M "
set -g status-left-length 30
set -g status-right-length 40

set -g window-status-format "#[fg=#928374] #I:#W "
set -g window-status-current-format "#[bg=#83a598,fg=#282828,bold] #I:#W "
set -g window-status-separator ""

set -g pane-border-style "fg=#928374"
set -g pane-active-border-style "fg=#b8bb26"
set -g message-style "bg=#282828,fg=#fabd2f"
set -g mode-style "bg=#cc241d,fg=#ebdbb2"

source-file -q ~/.cache/wal/colors-tmux.conf

# --- Plugins ---
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'

run '~/.tmux/plugins/tpm/tpm'

set-window-option -g mode-keys vi
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi y send-keys -X copy-selection-and-cancel

bind '"' split-window -v -c "#{pane_current_path}"
bind % split-window -h -c "#{pane_current_path}"
